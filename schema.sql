drop database cogent_do_connect;
create database cogent_do_connect;
use cogent_do_connect;
create table User(id int, name varchar(40), username varchar(40), password varchar(40), email varchar(40), userType varchar(40));